name: apigateway_dev_compose
services:
  frontend:
    image: inchanterx/faberge-frontend:develop
    pull_policy: always
    ports:
      - "3000:80"
    depends_on: 
      - gateway

  gateway:
    build: ../apigateway
    ports:
      - "8080:8080"
    depends_on:
      - auth
      - project

  auth:
    image: inchanterx/faberge-auth-service:develop
    pull_policy: always
    expose:
    - "8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  project:
    image: inchanterx/faberge-project-service:develop
    pull_policy: always
    expose:
    - "8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5